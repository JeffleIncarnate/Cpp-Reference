// Objects and Variables
#include <iostream>

int main() {
    int x; // Variable instantiation, the var has no value, but is ready to be used
    
    // Data types
    double age; // Variable instantiation but for a double, so we can store doubles

    // Defining multiple variables
    // int a;
    // int b;
    // is the same as
    int a, b;
    
    // However you CAN NOT do:
    // int a, double b;
    // But you can do:
    // int a; double b; (But not reccomended)
    // Just do this:
    // int a;
    // double b;

    return 0;
}
